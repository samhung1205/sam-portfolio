{
  "name": "sam-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Sam Hung 個人品牌網站（靜態站 + Notion 文章同步）",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "sync": "node scripts/sync-notion.mjs",
    "sync:local": "node --env-file=.env scripts/sync-notion.mjs",
    "sync:fixture": "node scripts/sync-notion.mjs --fixture",
    "verify": "node scripts/verify.mjs",
    "verify:fixture": "node scripts/verify-fixture.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0"
  }
}
